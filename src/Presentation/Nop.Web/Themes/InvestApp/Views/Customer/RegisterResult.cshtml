@model RegisterResultModel
@{
    Layout = "_ColumnsOne";

    //title
    NopHtml.AddTitleParts(T("PageTitle.Register").Text);
    //page class
    NopHtml.AppendPageCssClassParts("html-registration-result-page");
}
<div class="page registration-result-page">
    <!-- <div class="page-title">
        <h1>@T("Account.Register")</h1>
    </div>
    <div class="page-body">
        <div class="result">
            @Model.Result
        </div>
        <div class="buttons">
            <a href="@Model.ReturnUrl" class="button-1 register-continue-button">@T("Common.Continue")</a>
        </div>
    </div> -->

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-5">
                <div class="info-box">
                    <div class="child-box text-center py-2">
                        <div class="d-block mb-4">
                            <h2 class="mb-4">@T("Account.Register")</h2>
                            <h5 class="text-success mb-3">@Model.Result</h5>
                        </div>
                        <div class="buttons d-flex justify-content-center w-100">
                            <a href="@Model.ReturnUrl" class="button-1 register-continue-button">@T("Common.Continue")</a>
                        </div>
                    </div> 
                </div>
            </div>
        </div>
    </div>

</div>